import AppLayout from '@/components/layout/AppLayout';
import ComingSoonModule from './ComingSoonModule';

const WhatsAppSettings = () => (
  <AppLayout>
    <ComingSoonModule
      title="WhatsApp Cloud API"
      description="Meta API key, Phone Number ID, WhatsApp Business Account ID, webhook verification and encrypted credential storage — rebuilding fresh in step 2a."
    />
  </AppLayout>
);

export default WhatsAppSettings;
