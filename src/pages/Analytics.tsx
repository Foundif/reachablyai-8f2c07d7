import AppLayout from '@/components/layout/AppLayout';
import ComingSoonModule from './ComingSoonModule';

const Analytics = () => (
  <AppLayout>
    <ComingSoonModule
      title="Analytics"
      description="Message delivery, campaign performance and lead conversion — rebuilding on the new schema."
    />
  </AppLayout>
);

export default Analytics;
