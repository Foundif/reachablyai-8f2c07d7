import ComingSoonModule from './ComingSoonModule';

const Accounting = () => (
  <ComingSoonModule
    title="Accounting"
    description="Payment tracking and expense summaries — rebuilding on the new schema."
  />
);

export default Accounting;
