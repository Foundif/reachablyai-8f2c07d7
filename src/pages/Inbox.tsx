import AppLayout from '@/components/layout/AppLayout';
import ComingSoonModule from './ComingSoonModule';

const Inbox = () => (
  <AppLayout>
    <ComingSoonModule
      title="Team Inbox"
      description="Real-time WhatsApp conversations via Meta Cloud API, assignment, notes and labels — shipping in step 2c."
    />
  </AppLayout>
);

export default Inbox;
